const Partner = function (partner) {
    this.partner_id = partner.partner_id,
        this.squad_id = partner.squad_id,
        this.partner_squad_id = partner.partner_squad_id
};

module.exports = Partner;