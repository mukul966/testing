/*in production lets store this in an environment variable
This secret will be read by JWT library while creating and validating tokens
*/
module.exports = {
    secret: 'theelegentlordoftheringss'
};